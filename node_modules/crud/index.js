const mysql = require('mysql')

class ClassCrud {
    
    constructor() {
        this.sql = mysql.createConnection({
            host        : 'localhost',
            user        : 'curso',    
            database    : 'login',
            password    : '040303',
            port        : '3306'
        })
    }

    verificaLogin(_email, _senha) {
        this.sql.query('SELECT * FROM users', function(erro, results, fields){
            console.log(results[0]["celular"])
        })   
    }      
} 

module.exports = ClassCrud